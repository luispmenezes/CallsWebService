package api

import (
	"github.com/gin-gonic/gin"
)

func (b *BaseController) GetCallMetadata(c *gin.Context) {
	/*var startTime time.Time
	var endTime time.Time

	if startTime := c.Query("startTime"); len(startTime) > 0 {

	}
	endTime := c.Query("endTime")*/
}
